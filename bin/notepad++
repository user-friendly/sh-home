#!/bin/sh

if test ! -z "$1"; then
    file=$(cygpath -wlm "$1")
    echo "editing: $file"
else
    file=''
fi

/cygdrive/c/Program\ Files\ \(x86\)/Notepad++/notepad++.exe -multiInst -notabbar "$file"
